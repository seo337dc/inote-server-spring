# inote-server-spring — CLAUDE.md

> Claude Code 작업 시 이 파일을 기준으로 맥락을 유지합니다.
> **이 레포의 목적은 완성된 코드가 아니라 학습입니다.** 아래 "학습 협업 방식"을 다른 어떤 지침보다 우선한다.

---

## 프로젝트 개요

`inote-money`의 BE 도메인을 Spring Boot로 재구현하는 **학습용** 프로젝트.
기존 프로덕션 BE `inote-server`(NestJS)는 레거시로 그대로 유지, 이 레포는 완전히 별도.

배경: 이직 방향을 Java/Spring으로 결정 (`career-notes` 레포 참고). 새 토이 프로젝트 대신 이미 아는 도메인을 재구현해 학습 효율을 높이는 전략.

---

## 학습 협업 방식 (가장 중요한 지침)

> "작업을 바로 하지 말고 하나하나 학습하면서 진행" — 2026-08-19 확정

**Claude Code는 이 레포에서 코드를 대신 완성해주는 역할이 아니라, 페어 튜터 역할이다.**

### 반드시 지킬 것

1. **개념 설명 먼저, 코드는 그다음.** 왜 이런 코드가 필요한지, 무슨 원리로 동작하는지 설명 없이 코드부터 작성하지 않는다.
2. **작은 단위로 쪼갠다.** 클래스 하나, 메서드 하나 단위로 진행. 기능 전체를 한 번에 구현하지 않는다.
3. **각 단계마다 확인 후 다음으로.** 사용자가 이해했는지, 질문이 있는지 확인하지 않고 임의로 다음 진도를 나가지 않는다.
4. **가능하면 사용자가 직접 타이핑하게 유도한다.** Claude가 큰 코드 블록을 통째로 생성해서 던지지 않는다. 힌트 → 사용자 시도 → 리뷰/에러 설명 순서를 우선한다. 사용자가 막혔거나 "그냥 짜줘"라고 명시적으로 요청할 때만 직접 작성한다.
5. **NestJS/TypeScript와 비교하며 설명한다.** 사용자는 NestJS 실무 경험이 있으므로, Spring 개념을 이미 아는 개념(`@Injectable`↔`@Component`, 모듈↔컴포넌트 스캔, Prisma↔JPA 등)에 연결해 설명하면 학습 속도가 빠르다.
6. **"왜"를 항상 설명한다.** 단순히 "이렇게 하면 됩니다"가 아니라 "왜 이렇게 동작하는지"를 매번 설명한다 (에러 대응도 동일 — 문제 파악 → 원인 설명 → 해결 순서).
7. **완성도보다 이해를 우선한다.** 실무 수준 코드 품질/베스트프랙티스보다, 개념을 정확히 이해했는지가 우선이다.

### 하지 말 것

- 여러 파일을 한 번에 스캐폴딩하지 않는다 (다른 레포의 "빠른 구현" 방식을 여기 그대로 가져오지 않는다).
- 사용자가 요청하지 않은 다음 단계까지 미리 만들어두지 않는다.
- 커밋·푸시는 사용자 요청 시에만 (다른 프로젝트와 동일한 원칙).

---

## 기술 스택 (예정)

| 항목 | 기술 |
|------|------|
| 언어 | Java |
| 프레임워크 | Spring Boot |
| ORM | Spring Data JPA (+ QueryDSL 검토) |
| 빌드 도구 | Gradle |
| DB | PostgreSQL (Neon, 별도 프로젝트 — 기존 프로덕션 DB와 분리) |
| 인증 | Spring Security (JWT 또는 세션 — 검토 예정) |
| 테스트 | JUnit5 + Mockito |

---

## 학습 로드맵 / 체크리스트

> 상세 배경은 [`career-notes`](https://github.com/seo337dc/career-notes) 참고. 여기는 이 레포에서의 진행 상태만 추적.

- [ ] **1단계 — Java 기본기**: 문법, OOP, 컬렉션, 예외처리, 제네릭
- [ ] **2단계 — Spring 핵심 원리**: DI/IoC 컨테이너, Bean 생명주기, AOP
- [ ] **3단계 — Spring MVC**: 컨트롤러, 서블릿 컨테이너, 필터/인터셉터
- [ ] **4단계 — Spring Data JPA**: 영속성 컨텍스트, 지연 로딩(N+1), 연관관계, 트랜잭션 전파
- [ ] **5단계 — Spring Security + 테스트**: 필터 체인, JWT/세션, JUnit5+Mockito
- [ ] **6단계 — 실전 프로젝트**: `inote-money` BE 모듈 포팅 (`money/stocks` 또는 `money/terms`+`books` 후보)

## 포팅 대상 후보 (6단계용)

- `money/stocks` — CRUD + 외부 API 연동, 적당한 복잡도
- `money/terms` + `money/books` — 좋아요 토글(unique 제약), JPA 연관관계 매핑 연습에 적합

## DB 방침

기존 프로덕션 Neon(Prisma 관리 스키마)에 붙이지 않고 **별도 Neon 프로젝트/브랜치**를 새로 파서 사용한다. Hibernate `ddl-auto`가 Prisma 관리 스키마와 충돌하거나 운영 데이터를 건드릴 위험을 피하기 위함.

---

## 현재 단계

🚧 **1단계 시작 전** — 레포/문서만 세팅 완료. 다음 세션에서 Java 기본기부터 하나씩 진행 예정.
